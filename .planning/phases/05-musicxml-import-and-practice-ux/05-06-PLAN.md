---
phase: 05-musicxml-import-and-practice-ux
plan: "06"
type: execute
wave: 6
depends_on: ["05-05"]
files_modified:
  - tests/fixtures/tiny-score.musicxml
  - tests/fixtures/unsupported-score.musicxml
  - tests/browser-smoke/musicXmlImportPractice.test.js
  - tests/musicXmlParser.test.js
  - tests/musicXmlCanonicalAdapter.test.js
  - README.md
requirements: [XML-03, XML-04, XML-05, PRAC-01, PRAC-02, PRAC-03]
autonomous: true
must_haves:
  truths:
    - "Browser smoke proves accepted import, remembered library entry, removal, rendering, playback highlights, built-in and imported range loop, and auto-follow behavior."
    - "Regression tests prove strict validation and canonical mapping without brittle full SVG snapshots."
    - "Documentation explains static browser-only MusicXML import and local-storage limits."
  artifacts:
    - path: "tests/browser-smoke/musicXmlImportPractice.test.js"
      provides: "End-to-end Phase 5 user workflow smoke"
    - path: "tests/fixtures/tiny-score.musicxml"
      provides: "Accepted MusicXML fixture"
    - path: "tests/fixtures/unsupported-score.musicxml"
      provides: "Rejected MusicXML fixture"
    - path: "README.md"
      provides: "User-facing import/practice notes"
  key_links:
    - from: "tests/browser-smoke/musicXmlImportPractice.test.js"
      to: "index.html"
      via: "file upload, selector, range Shift+click, play/stop, remove"
      pattern: "setInputFiles"
    - from: "tests/*.test.js"
      to: "js/musicXmlParser.js and js/musicXmlCanonicalAdapter.js"
      via: "fixture-backed strict validation and canonical assertions"
      pattern: "tiny-score"
---

<objective>
Close Phase 5 with regression and browser smoke coverage for the complete MusicXML import and practice workflow.

Purpose: the user-facing promise is only trustworthy if import, validation, rendering, playback, range selection, auto-follow, persistence, and removal are tested as an integrated static-browser flow.
Output: fixtures, browser smoke, strengthened Node tests, and README notes.
</objective>

<execution_context>
@/home/mel/.codex/get-shit-done/workflows/execute-plan.md
@/home/mel/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-musicxml-import-and-practice-ux/05-CONTEXT.md
@.planning/phases/05-musicxml-import-and-practice-ux/05-UI-SPEC.md
@.planning/phases/05-musicxml-import-and-practice-ux/05-PATTERNS.md
@tests/browser-smoke/appBoot.test.js
@tests/musicXmlParser.test.js
@tests/musicXmlCanonicalAdapter.test.js
@README.md
</context>

<decision_coverage>
D-01 through D-17 are verified as a connected workflow: default complete-score surface, automatic persistence, duplicate suffixing, remove action, strict import, rejected unsupported parts/features, concise plus expandable errors, canonical mapping before display/playback, page-faithful scaling, interactive rendering, responsive page layout, Shift+click range, mint-green selection, start-from-measure, selected-range loop, and manual-scroll auto-follow pause/resume.
</decision_coverage>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add shared MusicXML fixtures and strengthen Node regressions</name>
  <files>tests/fixtures/tiny-score.musicxml, tests/fixtures/unsupported-score.musicxml, tests/musicXmlParser.test.js, tests/musicXmlCanonicalAdapter.test.js</files>
  <read_first>
    .planning/phases/05-musicxml-import-and-practice-ux/05-RESEARCH.md
    tests/musicXmlParser.test.js
    tests/musicXmlCanonicalAdapter.test.js
    js/musicXmlParser.js
    js/musicXmlCanonicalAdapter.js
  </read_first>
  <action>Create compact fixture files under tests/fixtures: tiny-score.musicxml for accepted piano-oriented score-partwise with page/system hints, notes, rests, chord, tie, backup/forward, key/time/clef; unsupported-score.musicxml for a fatal unsupported part/feature. Update Node tests to load these fixtures and assert strict diagnostics, canonical event order, measure/page metadata, and validateResolvedSequence results. Do not rely only on inline fixture strings after this task.</action>
  <acceptance_criteria>
    Fixtures are small enough to understand in code review and cover XML-04 semantics.
    Unsupported fixture produces fatal diagnostics and never creates a playable registered source.
    Accepted fixture produces canonical events, measures, and pageLayout suitable for rendering and playback.
  </acceptance_criteria>
  <done>Acceptance criteria pass and the automated verification command succeeds.</done>
  <verify>
    <automated>node --test tests/musicXmlParser.test.js tests/musicXmlCanonicalAdapter.test.js</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add end-to-end browser smoke for import and practice</name>
  <files>tests/browser-smoke/musicXmlImportPractice.test.js</files>
  <read_first>
    tests/browser-smoke/appBoot.test.js
    tests/fixtures/tiny-score.musicxml
    tests/fixtures/unsupported-score.musicxml
    .planning/phases/05-musicxml-import-and-practice-ux/05-UI-SPEC.md
  </read_first>
  <action>Create tests/browser-smoke/musicXmlImportPractice.test.js by reusing the static server and Chrome executable fallback pattern from appBoot.test.js. Test the full workflow: clear storage, verify default visible library is complete-score oriented, upload unsupported fixture and see short error plus expandable details, upload tiny-score fixture and see Imported "{title}", duplicate upload creates "{title} (2)", imported score appears after reload, score pages/SVG render, Shift+click measure range creates mint range badge, Play starts from selected range, loop toggle keeps playback inside range, manual scroll shows Auto-follow paused, Resume follow clears paused state, Stop clears transient highlights but keeps range, remove confirmation deletes only the imported score, and a built-in complete score such as Für Elise also exposes measure range selection/start/auto-follow behavior through staffNotationRenderer. Avoid full SVG snapshots.</action>
  <acceptance_criteria>
    Smoke asserts visible state and DOM behavior, not exact SVG serialization.
    Smoke covers persistence across reload and removal.
    Smoke covers practice range/start/auto-follow for at least one imported score and one built-in full-score source.
    Smoke fails if playback highlights or piano key active state are stuck after Stop.
  </acceptance_criteria>
  <done>Acceptance criteria pass and the automated verification command succeeds.</done>
  <verify>
    <automated>npm run test:smoke -- tests/browser-smoke/musicXmlImportPractice.test.js</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Document Phase 5 import and practice usage</name>
  <files>README.md</files>
  <read_first>
    README.md
    .planning/phases/05-musicxml-import-and-practice-ux/05-UI-SPEC.md
    docs/MUSICXML-ADAPTER.md
  </read_first>
  <action>Update README with concise usage notes for browser-only MusicXML import, local imported-score storage, duplicate suffixing, remove behavior, strict validation, selected-measure range looping, start-from-measure playback, and auto-follow pause/resume. State that imports are stored per browser origin and clearing site data removes them. Do not promise cloud sync, arbitrary orchestral support, lenient import, or note editing.</action>
  <acceptance_criteria>
    README mentions .musicxml/.xml import, local browser storage, strict validation, selected measure loop, auto-follow, and static hosting.
    README does not claim deferred features.
  </acceptance_criteria>
  <done>Acceptance criteria pass and the automated verification command succeeds.</done>
  <verify>
    <automated>rg "MusicXML|\\.musicxml|local|strict|selected range|Auto-follow" README.md && npm test && npm run test:smoke</automated>
  </verify>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser smoke fixture upload to app | Test exercises untrusted import path |
| persisted imported library to reload | Stored local data is restored into UI and renderer |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | Tampering | smoke fixtures | mitigate | Test both accepted and unsupported files so strict rejection and accepted mapping remain covered. |
| T-05-06-02 | Denial of Service | browser smoke playback | mitigate | Assert Stop clears highlights and active keys after range playback. |
| T-05-06-03 | Information Disclosure | README | accept | Document browser-local storage behavior; no network transfer is introduced. |
</threat_model>

<verification>
Run npm test and npm run test:smoke. Confirm no test uses brittle full SVG snapshots.
</verification>

<success_criteria>
All Phase 5 requirements XML-03, XML-04, XML-05, PRAC-01, PRAC-02, and PRAC-03 are covered by fixtures, Node tests, and browser smoke proving the complete static-browser import/practice workflow.
</success_criteria>

<output>
After completion, create .planning/phases/05-musicxml-import-and-practice-ux/05-06-SUMMARY.md
</output>
