---
phase: 05-musicxml-import-and-practice-ux
plan: "03"
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - js/musicXmlCanonicalAdapter.js
  - js/patternValidator.js
  - js/musicXmlAdapterContract.js
  - js/player.js
  - tests/playerRange.test.js
  - tests/musicXmlCanonicalAdapter.test.js
requirements: [XML-04, XML-05, PRAC-02]
autonomous: true
must_haves:
  truths:
    - "Accepted MusicXML maps cleanly to canonical events before display or playback per D-08."
    - "MusicXML notes, rests, chords, ties, accidentals, clefs, key signatures, time signatures, backups, forwards, and layout metadata are validated or rejected per XML-04."
    - "Player can start from a selected measure/range without creating a MusicXML-only playback path per D-15."
  artifacts:
    - path: "js/musicXmlCanonicalAdapter.js"
      provides: "MusicXML-to-canonical event and measure adapter"
    - path: "tests/musicXmlCanonicalAdapter.test.js"
      provides: "Coverage for cursor semantics, timing, chords, rests, ties, measures, and layout"
    - path: "js/player.js"
      provides: "Range/start-aware playback using canonical events"
    - path: "tests/playerRange.test.js"
      provides: "Focused coverage for range start/end and loop wrapping behavior"
  key_links:
    - from: "js/musicXmlCanonicalAdapter.js"
      to: "js/patternValidator.js"
      via: "validateResolvedSequence plus MusicXML measure/page validation"
      pattern: "validateResolvedSequence"
    - from: "js/player.js"
      to: "canonical sequence events"
      via: "play(sequence, { loop, range })"
      pattern: "range"
---

<objective>
Convert strict MusicXML parse results into the existing canonical score/event model and make playback range/start aware.

Purpose: imported scores must be playable through the existing piano/highlight scheduler, not a separate MusicXML playback path.
Output: adapter, validation extensions, contract updates, player range support, and Node tests.
</objective>

<execution_context>
@/home/mel/.codex/get-shit-done/workflows/execute-plan.md
@/home/mel/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-musicxml-import-and-practice-ux/05-CONTEXT.md
@.planning/phases/05-musicxml-import-and-practice-ux/05-RESEARCH.md
@.planning/phases/05-musicxml-import-and-practice-ux/05-PATTERNS.md
@docs/MUSICXML-ADAPTER.md
@js/musicXmlParser.js
@js/canonicalPatternResolver.js
@js/patternValidator.js
@js/musicXmlAdapterContract.js
@js/player.js
@tests/canonicalPatternResolver.test.js
@tests/patternValidator.test.js
</context>

<decision_coverage>
D-05: multi-part support is attempted only when it maps all supported parts; unsupported parts are fatal.
D-06: unsupported MusicXML semantics are rejected in strict mode.
D-08: adapter output must pass canonical validation before any accepted display/playback.
D-15: playback begins from selected measure/range start first; note/event start is optional only if reliable IDs exist.
D-16: range support is implemented in canonical player options so the existing loop toggle can use it later.
</decision_coverage>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add canonical adapter behavior tests</name>
  <files>tests/musicXmlCanonicalAdapter.test.js</files>
  <read_first>
    .planning/phases/05-musicxml-import-and-practice-ux/05-RESEARCH.md
    .planning/phases/05-musicxml-import-and-practice-ux/05-PATTERNS.md
    tests/canonicalPatternResolver.test.js
    tests/patternValidator.test.js
    docs/MUSICXML-ADAPTER.md
  </read_first>
  <action>Create tests/musicXmlCanonicalAdapter.test.js with compact fixture strings for quarter/eighth durations, rests, chords using MusicXML chord semantics, ties, accidentals, clef/key/time attributes, backup/forward cursor movement, measure metadata, pageLayout print hints, and unsupported multi-part rejection. Assert stable event IDs, ordered startBeat values, durationBeats in quarter-note units, left/right hand payloads, eventIds per measure, and fatal-free validateResolvedSequence output for accepted fixtures.</action>
  <acceptance_criteria>
    Tests fail before js/musicXmlCanonicalAdapter.js exists.
    Tests include at least one fixture where append-only parsing would place events incorrectly because of backup/forward.
    Tests assert unsupported parts are rejected, not silently skipped.
  </acceptance_criteria>
  <done>Acceptance criteria pass and the automated verification command succeeds.</done>
  <verify>
    <automated>node --test tests/musicXmlCanonicalAdapter.test.js</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement MusicXML canonical adapter and validation extensions</name>
  <files>js/musicXmlCanonicalAdapter.js, js/patternValidator.js, js/musicXmlAdapterContract.js, tests/musicXmlCanonicalAdapter.test.js</files>
  <read_first>
    tests/musicXmlCanonicalAdapter.test.js
    js/musicXmlParser.js
    js/canonicalPatternResolver.js
    js/patternValidator.js
    js/musicXmlAdapterContract.js
  </read_first>
  <action>Create js/musicXmlCanonicalAdapter.js as a pure named-export module with adaptMusicXmlDocumentToCanonical and validateMusicXmlCanonicalScore. Maintain cursor state for divisions, part, measure, voice/staff, backups, forwards, and chord same-onset grouping. Convert duration/divisions into durationBeats where 1 is a quarter note. Extend patternValidator with measure/page consistency checks needed by MusicXML without weakening existing pattern validation. Update musicXmlAdapterContract implemented/deferred fields to reflect Phase 5 parser and adapter support.</action>
  <acceptance_criteria>
    Accepted adapter output contains sourceId, sourceType musicxml, metadata, timeSignature, beatsPerMeasure, loopUnitBeats, events, measures, and pageLayout.measureLayout.
    validateResolvedSequence remains fatal-free for accepted fixtures and still fails invalid canonical sequences.
    Unsupported MusicXML features produce structured fatal diagnostics with useful code/path/message.
  </acceptance_criteria>
  <done>Acceptance criteria pass and the automated verification command succeeds.</done>
  <verify>
    <automated>node --check js/musicXmlCanonicalAdapter.js js/patternValidator.js js/musicXmlAdapterContract.js && node --test tests/musicXmlCanonicalAdapter.test.js tests/patternValidator.test.js tests/musicXmlAdapterContract.test.js</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add canonical range/start playback support</name>
  <files>js/player.js, tests/playerRange.test.js, tests/musicXmlCanonicalAdapter.test.js</files>
  <read_first>
    js/player.js
    js/musicXmlCanonicalAdapter.js
    tests/canonicalPatternResolver.test.js
    .planning/phases/05-musicxml-import-and-practice-ux/05-CONTEXT.md
  </read_first>
  <action>Create tests/playerRange.test.js with focused Player.play(sequence, { loop, range }) coverage using stubbed audioEngine, piano, and settings objects. Cover no-range behavior, measure-range start, stop after end measure when loop is false, and loop wrapping to the range start when loop is true. Then extend Player.play(sequence, { loop, range }) so range may specify startMeasureNumber/endMeasureNumber or startEventId/endEventId. Resolve the range against canonical sequence.events and sequence.measures at setup, preserve onNoteHighlight(event.id, event), preserve stop cleanup, and make loop wrap to the range start when loop is enabled. Do not add renderer-specific playback branches.</action>
  <acceptance_criteria>
    Playback without range behaves as before.
    Playback with a measure range starts at the first event in the start measure and stops or loops after the end measure.
    Existing loop toggle semantics remain available through setLoopEnabled and play options.
    tests/playerRange.test.js asserts measure-range start, end stop, and loop wrap behavior directly rather than relying only on browser smoke.
  </acceptance_criteria>
  <done>Acceptance criteria pass and the automated verification command succeeds.</done>
  <verify>
    <automated>node --check js/player.js && node --test tests/playerRange.test.js && npm test</automated>
  </verify>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| parsed XML to canonical adapter | Untrusted but parsed score data becomes playable model data |
| canonical model to player | Imported events can schedule audio and visual highlights |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Tampering | js/musicXmlCanonicalAdapter.js | mitigate | Validate all accepted events through canonical and MusicXML-specific checks before playback. |
| T-05-03-02 | Denial of Service | js/player.js | mitigate | Reject empty/invalid ranges and avoid scheduling unbounded or zero-duration loops. |
| T-05-03-03 | Information Disclosure | diagnostics | accept | Diagnostics describe local user-provided files only and are not transmitted. |
</threat_model>

<verification>
Run node --test tests/musicXmlCanonicalAdapter.test.js tests/patternValidator.test.js tests/musicXmlAdapterContract.test.js and npm test.
</verification>

<success_criteria>
MusicXML fixtures convert into canonical events/measures/page metadata, unsupported structures fail strictly, and Player can start and loop over canonical ranges without bypassing existing piano/highlight behavior.
</success_criteria>

<output>
After completion, create .planning/phases/05-musicxml-import-and-practice-ux/05-03-SUMMARY.md
</output>
