---
phase: 05-musicxml-import-and-practice-ux
plan: "04"
type: execute
wave: 4
depends_on: ["05-01", "05-03"]
files_modified:
  - js/musicXmlScoreRenderer.js
  - js/staffNotationRenderer.js
  - index.html
  - css/styles.css
  - css/mobile.css
  - tests/browser-smoke/musicXmlImportUi.test.js
requirements: [XML-03, XML-05]
autonomous: true
must_haves:
  truths:
    - "D-01: Default library surface shows complete score-like entries first after clear browser storage."
    - "Imported MusicXML scores render as complete sheet pages and remain selectable in the app."
    - "Page and system layout is preserved where MusicXML provides it; incomplete layout generates full pages instead of arbitrary flow."
    - "Playback highlights can target rendered imported-score SVG/DOM nodes."
    - "D-07: Import failures show a short toast and expandable user-facing import details."
  artifacts:
    - path: "js/musicXmlScoreRenderer.js"
      provides: "Production imported-score page renderer"
    - path: "index.html"
      provides: "Import rendering, startup restoration, selector/library wiring, and remove controls"
    - path: "tests/browser-smoke/musicXmlImportUi.test.js"
      provides: "Focused browser proof for import details, restore-on-reload, and removal UI"
    - path: "css/styles.css"
      provides: "Desktop two-page stand, import/library surface, validation details, and page scaling styles"
    - path: "css/mobile.css"
      provides: "Mobile one-page vertical score layout and wrapping import details"
  key_links:
    - from: "index.html"
      to: "js/musicXmlScoreRenderer.js"
      via: "cache-busted dynamic import and render dispatch for sourceType musicxml"
      pattern: "musicXmlScoreRenderer"
    - from: "js/musicXmlScoreRenderer.js"
      to: "Player.onNoteHighlight"
      via: "eventMap returned to index highlightNotationNote"
      pattern: "eventMap"
---

<objective>
Wire imported MusicXML scores into the visible score library and render them as page-faithful interactive sheets.

Purpose: the import core becomes user-visible only after it can display accepted scores and preserve the app's notation/playback trust contract.
Output: production imported-score renderer, import/library UI wiring, page layout CSS, and validation-detail display states.
</objective>

<execution_context>
@/home/mel/.codex/get-shit-done/workflows/execute-plan.md
@/home/mel/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-musicxml-import-and-practice-ux/05-CONTEXT.md
@.planning/phases/05-musicxml-import-and-practice-ux/05-RESEARCH.md
@.planning/phases/05-musicxml-import-and-practice-ux/05-UI-SPEC.md
@.planning/phases/05-musicxml-import-and-practice-ux/05-PATTERNS.md
@docs/PHASE5-RENDERER-STORAGE-GATE.md
@js/musicXmlParser.js
@js/musicXmlCanonicalAdapter.js
@js/importedScoreStore.js
@js/importedScoreLibrary.js
@js/musicXmlScoreRenderer.js
@js/staffNotationRenderer.js
@index.html
@css/styles.css
@css/mobile.css
@tests/browser-smoke/appBoot.test.js
</context>

<decision_coverage>
D-01: default selector/library surface shows complete score-like pieces after clear browser storage.
D-02, D-03, D-04: successful imports are remembered, duplicate-suffixed, and removable near the library UI.
D-07: failed imports show a short toast plus expandable import details.
D-09, D-10, D-11, D-12: rendered imported scores are full pages, scaled without reflow, interactive, two-page on desktop, one-page vertical on small screens.
D-14: mint accent is reserved for range display and not used for generic import success.
</decision_coverage>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Wire MusicXML import and removal UI</name>
  <files>index.html, css/styles.css, css/mobile.css, tests/browser-smoke/musicXmlImportUi.test.js</files>
  <read_first>
    .planning/phases/05-musicxml-import-and-practice-ux/05-UI-SPEC.md
    js/musicXmlParser.js
    js/musicXmlCanonicalAdapter.js
    js/importedScoreStore.js
    js/importedScoreLibrary.js
    index.html
    css/styles.css
    css/mobile.css
    tests/browser-smoke/appBoot.test.js
  </read_first>
  <action>Add an Import MusicXML control near the score selector/library, accepting .musicxml and .xml. On app startup, before the initial selector/library render, restore persisted imported records by calling listImportedScores()/getImportedScore(), register valid restored records through importedScoreLibrary, update the selector/library, and handle stale/corrupted records with visible import-library diagnostics rather than crashing. On file selection, read text with Blob.text(), parse strictly, adapt to canonical, save to IndexedDB, register through importedScoreLibrary, select or visibly add the imported score, and show Imported "{title}" toast. Add visible remove action for imported entries only with the UI-SPEC destructive confirmation copy. Add expandable details using details/summary with Show import details and Import details copy for failed imports and storage failures. Add focused browser smoke coverage in tests/browser-smoke/musicXmlImportUi.test.js for failed import details, successful import, reload restoration, and removal confirmation. Keep controls out of the sound panel.</action>
  <acceptance_criteria>
    Import control has an accessible name and accepts .musicxml,.xml.
    Failed import displays the UI-SPEC error state plus expandable details; accepted import cannot appear unless storage and canonical validation succeed.
    Remove action is visible near imported score selection, uses destructive color, has at least 44px target, and does not affect built-in scores.
    Previously saved imported scores are restored from IndexedDB into the selector/library on app boot before initial render.
    Stale or corrupted stored records produce visible diagnostics and do not crash the app.
    tests/browser-smoke/musicXmlImportUi.test.js proves failed import details, successful import, restore after reload, and remove confirmation behavior.
  </acceptance_criteria>
  <done>Acceptance criteria pass and the automated verification command succeeds.</done>
  <verify>
    <automated>node --check js/musicXmlParser.js js/musicXmlCanonicalAdapter.js js/importedScoreStore.js js/importedScoreLibrary.js && rg "Import MusicXML|Show import details|Remove imported score|listImportedScores|getImportedScore|\\.musicxml|\\.xml" index.html css/styles.css css/mobile.css && npm run test:smoke -- tests/browser-smoke/musicXmlImportUi.test.js</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render imported score pages through renderer facade</name>
  <files>js/musicXmlScoreRenderer.js, js/staffNotationRenderer.js, index.html</files>
  <read_first>
    docs/PHASE5-RENDERER-STORAGE-GATE.md
    js/musicXmlScoreRenderer.js
    js/staffNotationRenderer.js
    js/musicXmlCanonicalAdapter.js
    index.html
  </read_first>
  <action>Complete renderMusicXmlScore so sourceType musicxml selections render full pages from canonical pageLayout and renderer-gate path. Preserve file page/system hints where present; when incomplete, generate full .score-page pages instead of arbitrary flowing measures. Return eventMap, measureMap, sequence, and pages to index.html. Update index.html render dispatch so built-in patterns continue using existing drawStaffNotation while imported scores use renderMusicXmlScore.</action>
  <acceptance_criteria>
    Imported score render result includes non-empty pages and eventMap for accepted fixtures.
    Built-in short patterns still render through the existing path.
    No static-image-only rendering is introduced.
  </acceptance_criteria>
  <done>Acceptance criteria pass and the automated verification command succeeds.</done>
  <verify>
    <automated>node --check js/musicXmlScoreRenderer.js js/staffNotationRenderer.js && npm test</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Apply page-faithful responsive layout styles</name>
  <files>css/styles.css, css/mobile.css</files>
  <read_first>
    .planning/phases/05-musicxml-import-and-practice-ux/05-UI-SPEC.md
    css/styles.css
    css/mobile.css
  </read_first>
  <action>Add Phase 5 styles for library/import status, validation details, imported remove action, score page viewport, and renderer facade containers. Preserve .staff-notation, #vexflow-notation, .score-sheet-view, .score-page-grid, and .score-page visual language. Desktop/wide layout uses two-page stand with 32px gap; mobile <=768px uses one-page vertical layout and vertical scrolling. Maintain bottom padding using --piano-bottom-space so the fixed keyboard never covers import controls, validation details, or active score pages.</action>
  <acceptance_criteria>
    New colors follow UI-SPEC: mint #6ee7b7 only for range-related UI, destructive #c53030 for remove/fatal import.
    Long filenames and diagnostics wrap with overflow-wrap anywhere.
    No cards are nested inside the sheet area; pages remain the framed objects.
  </acceptance_criteria>
  <done>Acceptance criteria pass and the automated verification command succeeds.</done>
  <verify>
    <automated>rg "#6ee7b7|#c53030|score-page-grid|piano-bottom-space|overflow-wrap" css/styles.css css/mobile.css</automated>
  </verify>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser file picker to app UI | Untrusted file text is user-selected |
| renderer output to DOM | Imported score data becomes visible DOM/SVG |
| IndexedDB to selector | Stored imported metadata re-enters UI after refresh |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Spoofing | imported score titles | mitigate | Suffix duplicates and mark imported entries so local records are distinguishable. |
| T-05-04-02 | Tampering | remove action | mitigate | Confirmation applies only to imported records and must not touch built-ins/settings. |
| T-05-04-03 | Elevation of Privilege | import UI | mitigate | File text goes through parser/adapter/store only; no executable uploaded patterns. |
</threat_model>

<verification>
Run npm test. Later Plan 06 will add full browser smoke; this plan must still pass node checks and source assertions.
</verification>

<success_criteria>
Users can import, see, select, and remove local MusicXML scores, failures are visible and expandable, and accepted imports render as complete page-faithful interactive sheets without breaking built-in pattern display.
</success_criteria>

<output>
After completion, create .planning/phases/05-musicxml-import-and-practice-ux/05-04-SUMMARY.md
</output>
