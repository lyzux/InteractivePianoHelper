---
phase: 05-musicxml-import-and-practice-ux
plan: "05"
type: execute
wave: 5
depends_on: ["05-04"]
files_modified:
  - js/practiceRangeController.js
  - js/autoFollowController.js
  - js/player.js
  - index.html
  - css/styles.css
  - css/mobile.css
  - tests/browser-smoke/practiceRangeControls.test.js
requirements: [XML-05, PRAC-01, PRAC-02, PRAC-03]
autonomous: true
must_haves:
  truths:
    - "D-13: User can Shift+click rendered measures to choose a normalized measure range; plain click does not create a range."
    - "D-14: Selected range is visibly mint-green and distinct from amber playback highlights."
    - "D-16: Existing loop toggle loops the selected range when present and the full score when no range exists."
    - "D-17: Auto-scroll follows playback and pauses when the user manually scrolls away until resumed."
  artifacts:
    - path: "js/practiceRangeController.js"
      provides: "Range state, Shift+click binding, clear range, and playback range contract"
    - path: "js/autoFollowController.js"
      provides: "Playback-system follow behavior with manual-scroll pause/resume"
    - path: "js/player.js"
      provides: "Range-aware canonical playback integration"
    - path: "index.html"
      provides: "Practice controls wired to current notation maps and player"
    - path: "tests/browser-smoke/practiceRangeControls.test.js"
      provides: "Focused browser proof for range persistence, non-Shift selection alternative, playback range wiring, and auto-follow pause/resume"
  key_links:
    - from: "js/practiceRangeController.js"
      to: "js/musicXmlScoreRenderer.js"
      via: "measureMap entries from currentNotationMaps"
      pattern: "measureMap"
    - from: "js/practiceRangeController.js"
      to: "js/staffNotationRenderer.js"
      via: "measureMap entries from built-in full-score render output"
      pattern: "measureMap"
    - from: "js/autoFollowController.js"
      to: "Player.onNoteHighlight"
      via: "event callback receives canonical event page/system metadata"
      pattern: "onNoteHighlight"
---

<objective>
Add score-based practice controls for selected-measure looping, start-from-measure playback, and auto-follow.

Purpose: imported and built-in score content becomes practiceable, not just viewable, while preserving existing Play, Loop, sound controls, piano highlights, and notation highlight behavior.
Output: range controller, auto-follow controller, player/UI integration, and responsive practice styling.
</objective>

<execution_context>
@/home/mel/.codex/get-shit-done/workflows/execute-plan.md
@/home/mel/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-musicxml-import-and-practice-ux/05-CONTEXT.md
@.planning/phases/05-musicxml-import-and-practice-ux/05-UI-SPEC.md
@.planning/phases/05-musicxml-import-and-practice-ux/05-PATTERNS.md
@js/musicXmlScoreRenderer.js
@js/staffNotationRenderer.js
@js/player.js
@index.html
@css/styles.css
@css/mobile.css
@tests/browser-smoke/appBoot.test.js
</context>

<decision_coverage>
D-13: range selection uses Shift+click on rendered measures; plain click does not create ranges.
D-14: selected range uses mint-green #6ee7b7 and remains visually distinct from amber playback highlights.
D-15: playback starts from selected measure/range start.
D-16: existing loop toggle loops selected range when present, otherwise full score.
D-17: auto-follow follows playback until manual scroll pauses it; Resume follow or playback restart resumes it.
</decision_coverage>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement practice range controller and UI state</name>
  <files>js/practiceRangeController.js, js/staffNotationRenderer.js, index.html, css/styles.css, css/mobile.css, tests/browser-smoke/practiceRangeControls.test.js</files>
  <read_first>
    .planning/phases/05-musicxml-import-and-practice-ux/05-UI-SPEC.md
    js/musicXmlScoreRenderer.js
    js/staffNotationRenderer.js
    index.html
    css/styles.css
    css/mobile.css
    tests/browser-smoke/appBoot.test.js
  </read_first>
  <action>Create js/practiceRangeController.js with named export createPracticeRangeController. It must accept current notation maps from both js/musicXmlScoreRenderer.js and js/staffNotationRenderer.js, bind Shift+click to measureMap DOM targets, set first click as range start, second click as range end, normalize reversed ranges, expose clearRange and getPlaybackRange, and emit UI updates for "Measures {start}-{end} selected" plus "Clear range". Extend staffNotationRenderer output for built-in full-score sources to include measureMap entries, pageNumber/systemIndex metadata, and measure DOM targets compatible with practiceRangeController. Add a mandatory keyboard-accessible and touch-friendly range-selection alternative, such as an explicit range mode with 44px start/end controls or equivalent accessible controls, so users without Shift+click can select the same normalized measure range. Plain click must not create a range unless the explicit range mode is active. Add focused browser smoke coverage in tests/browser-smoke/practiceRangeControls.test.js for Shift+click range selection, non-Shift/range-mode selection, range persistence after stop, and Clear range.</action>
  <acceptance_criteria>
    Selected range persists after playback stop and clears only through Clear range or source change.
    Mint-green accent is used for range overlays/boundaries/badge only.
    Range selection has non-color indicators through badge text and/or boundary marks.
    Built-in full-score output from staffNotationRenderer exposes measureMap/page-system metadata usable by practiceRangeController.
    Non-Shift users can select a range through explicit keyboard/touch-accessible controls with at least 44px targets.
    tests/browser-smoke/practiceRangeControls.test.js proves Shift+click selection, non-Shift alternative selection, range persistence after stop, and Clear range behavior.
  </acceptance_criteria>
  <done>Acceptance criteria pass and the automated verification command succeeds.</done>
  <verify>
    <automated>node --check js/practiceRangeController.js js/staffNotationRenderer.js && rg "Measures .* selected|Clear range|#6ee7b7|shiftKey|measureMap|systemIndex|pageNumber|range mode|aria|44px" js/practiceRangeController.js js/staffNotationRenderer.js index.html css/styles.css css/mobile.css && npm run test:smoke -- tests/browser-smoke/practiceRangeControls.test.js</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire range-aware playback and loop behavior</name>
  <files>js/player.js, index.html, tests/browser-smoke/practiceRangeControls.test.js</files>
  <read_first>
    js/player.js
    js/practiceRangeController.js
    index.html
    tests/browser-smoke/practiceRangeControls.test.js
    .planning/phases/05-musicxml-import-and-practice-ux/05-CONTEXT.md
  </read_first>
  <action>Update index.html play/stop wiring so handlePlayStop asks practiceRangeController.getPlaybackRange() and passes that range to player.play(sequence, { loop, range }). Existing loop checkbox remains the only loop toggle; when a range exists it loops the selected range, otherwise the full score. Starting playback with a selected range begins at the selected range start measure. Stop clears transient playback highlights but keeps selected range.</action>
  <acceptance_criteria>
    Playback with no range follows pre-Phase-5 behavior.
    Playback with a selected range starts from the range start measure and respects loop toggle.
    Loop helper text says "Loop plays the selected range when one is selected."
    tests/browser-smoke/practiceRangeControls.test.js proves Play starts from selected range and Stop keeps the selected range visible.
  </acceptance_criteria>
  <done>Acceptance criteria pass and the automated verification command succeeds.</done>
  <verify>
    <automated>node --check js/player.js && rg "getPlaybackRange|player.play\\(sequence, \\{ loop, range \\}\\)|Loop plays the selected range" index.html js/player.js && npm run test:smoke -- tests/browser-smoke/practiceRangeControls.test.js</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement auto-follow pause and resume</name>
  <files>js/autoFollowController.js, js/staffNotationRenderer.js, index.html, css/styles.css, css/mobile.css, tests/browser-smoke/practiceRangeControls.test.js</files>
  <read_first>
    .planning/phases/05-musicxml-import-and-practice-ux/05-UI-SPEC.md
    js/player.js
    js/musicXmlScoreRenderer.js
    js/staffNotationRenderer.js
    index.html
    css/styles.css
    css/mobile.css
    tests/browser-smoke/practiceRangeControls.test.js
  </read_first>
  <action>Create js/autoFollowController.js with named export createAutoFollowController. It must receive the score viewport, current notation maps from imported and built-in score renderers, and playback events, scroll the current page/system into view while playback is active, pause follow state on manual user scroll, show "Auto-follow paused" plus "Resume follow", and resume when the user clicks Resume follow or playback restarts. Ensure built-in full-score output from staffNotationRenderer supplies page/system metadata needed by autoFollowController. It must scroll the score viewport/system only and not the fixed bottom keyboard or sound panel.</action>
  <acceptance_criteria>
    Auto-follow active state scrolls to the current playback system/page using event metadata.
    Auto-follow works with built-in score output from staffNotationRenderer as well as imported MusicXML output.
    Manual scroll pauses auto-follow and does not fight the user.
    Resume follow restores auto-follow and uses mint accent only for the resume affordance when paused.
    tests/browser-smoke/practiceRangeControls.test.js proves manual scroll shows Auto-follow paused and Resume follow restores follow state.
  </acceptance_criteria>
  <done>Acceptance criteria pass and the automated verification command succeeds.</done>
  <verify>
    <automated>node --check js/autoFollowController.js js/staffNotationRenderer.js && rg "Auto-follow paused|Resume follow|scrollIntoView|manual|autoFollow|systemIndex|pageNumber" js/autoFollowController.js js/staffNotationRenderer.js index.html css/styles.css css/mobile.css && npm run test:smoke -- tests/browser-smoke/practiceRangeControls.test.js</automated>
  </verify>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| score DOM to practice controller | Rendered measure/event metadata drives playback range |
| user scroll/click to playback | User interactions affect scheduler start/loop bounds |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Tampering | js/practiceRangeController.js | mitigate | Normalize and validate measure ranges against current canonical measures before playback. |
| T-05-05-02 | Denial of Service | js/player.js | mitigate | Reject empty ranges and zero-duration loops; preserve stop cleanup timers. |
| T-05-05-03 | Spoofing | auto-follow UI | mitigate | Show explicit paused/resume state so automatic scroll behavior is clear to the user. |
</threat_model>

<verification>
Run npm test and source assertions above. Full interaction smoke lands in Plan 06.
</verification>

<success_criteria>
Users can select measure ranges, see mint-green range feedback, start playback from a selected measure, loop selected ranges with the existing loop toggle, and control auto-follow after manual scrolling.
</success_criteria>

<output>
After completion, create .planning/phases/05-musicxml-import-and-practice-ux/05-05-SUMMARY.md
</output>
