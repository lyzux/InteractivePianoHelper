---
phase: 05-musicxml-import-and-practice-ux
plan: "02"
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - js/musicXmlParser.js
  - js/importedScoreLibrary.js
  - js/simplePatternLoader.js
  - tests/musicXmlParser.test.js
requirements: [XML-03, XML-04]
autonomous: true
must_haves:
  truths:
    - "D-01: Default library filtering supports complete score-like pieces as the primary visible surface while preserving short pedagogical patterns internally."
    - "User-imported .musicxml and .xml text is parsed as inert data and rejected clearly when malformed or unsupported."
    - "Accepted imports are prepared for automatic local registration and duplicate suffixing per D-02 and D-03."
    - "Strict import is the only Phase 5 mode per D-06; unsupported parts/features are rejected per D-05."
  artifacts:
    - path: "js/musicXmlParser.js"
      provides: "Strict data-only parser and structure validator"
    - path: "js/importedScoreLibrary.js"
      provides: "Imported source registration, duplicate title suffixing, and loader integration helpers"
    - path: "tests/musicXmlParser.test.js"
      provides: "Node contract coverage for malformed XML, unsupported roots, missing structure, diagnostics, and duplicate suffixing"
  key_links:
    - from: "js/importedScoreLibrary.js"
      to: "js/simplePatternLoader.js"
      via: "sourceType musicxml registration and rejected-source diagnostics"
      pattern: "sourceType.*musicxml"
    - from: "js/musicXmlParser.js"
      to: "js/patternValidator.js"
      via: "createDiagnostic-compatible result diagnostics"
      pattern: "createDiagnostic"
---

<objective>
Create the strict MusicXML import core that turns file text into validated parse results and importable library records, without display-only or executable user content.

Purpose: Phase 5 import must fail early, explain failures clearly, and integrate with the existing valid/rejected source registry before UI or playback depends on it.
Output: parser, imported-library registration helpers, loader extension, and focused Node tests.
</objective>

<execution_context>
@/home/mel/.codex/get-shit-done/workflows/execute-plan.md
@/home/mel/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-musicxml-import-and-practice-ux/05-CONTEXT.md
@.planning/phases/05-musicxml-import-and-practice-ux/05-RESEARCH.md
@.planning/phases/05-musicxml-import-and-practice-ux/05-UI-SPEC.md
@.planning/phases/05-musicxml-import-and-practice-ux/05-PATTERNS.md
@docs/MUSICXML-ADAPTER.md
@js/musicXmlAdapterContract.js
@js/patternValidator.js
@js/simplePatternLoader.js
@js/importedScoreStore.js
@tests/musicXmlAdapterContract.test.js
</context>

<decision_coverage>
D-01: imported/library helpers must support a default complete-score library surface rather than making short patterns primary.
D-02: successful imports are shaped for automatic local persistence through Plan 01 store.
D-03: duplicate titles use "{title} (2)", "{title} (3)" before registration.
D-05, D-06: unsupported multi-part/unsupported features are rejected in strict mode.
D-07: failures produce concise user messages plus structured expandable diagnostics.
D-08: parse results must be ready for canonical mapping; no display-only accepted import.
</decision_coverage>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add strict MusicXML parser tests</name>
  <files>tests/musicXmlParser.test.js</files>
  <read_first>
    .planning/phases/05-musicxml-import-and-practice-ux/05-CONTEXT.md
    .planning/phases/05-musicxml-import-and-practice-ux/05-RESEARCH.md
    docs/MUSICXML-ADAPTER.md
    tests/musicXmlAdapterContract.test.js
    tests/patternValidator.test.js
  </read_first>
  <action>Create tests/musicXmlParser.test.js using node:test and node:assert/strict. Cover accepted score-partwise fixture text, malformed XML/parsererror, rejected score-timewise, missing part-list, missing parts, empty measures, unsupported multi-part structures, and diagnostic source identity with sourceType musicxml. Include tests for duplicate suffix selection in importedScoreLibrary.</action>
  <acceptance_criteria>
    Tests assert exact diagnostic codes for parse failure, unsupported root, missing part-list, missing measure, unsupported part, and duplicate suffix behavior.
    Tests fail before js/musicXmlParser.js and js/importedScoreLibrary.js are implemented.
  </acceptance_criteria>
  <done>Acceptance criteria pass and the automated verification command succeeds.</done>
  <verify>
    <automated>node --test tests/musicXmlParser.test.js</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement strict parser and diagnostics</name>
  <files>js/musicXmlParser.js, tests/musicXmlParser.test.js</files>
  <read_first>
    tests/musicXmlParser.test.js
    js/musicXmlAdapterContract.js
    js/patternValidator.js
    docs/MUSICXML-ADAPTER.md
  </read_first>
  <action>Create js/musicXmlParser.js as a pure named-export module. Export parseMusicXmlText and validateMusicXmlDocument. Use DOMParser when available and an injectable parser for Node tests. Return { ok, document, descriptor, diagnostics } and never throw for expected user file failures. Detect parsererror, require score-partwise, part-list, part, measure, attributes needed for supported playback, and reject unsupported roots/features with createDiagnostic-compatible diagnostics.</action>
  <acceptance_criteria>
    Malformed or unsupported XML returns ok false with diagnostics, not uncaught exceptions.
    Parser has no eval, Function, dynamic import of user content, script execution, or remote fetch of uploaded content.
    Accepted fixture returns ok true with a descriptor carrying sourceType musicxml.
  </acceptance_criteria>
  <done>Acceptance criteria pass and the automated verification command succeeds.</done>
  <verify>
    <automated>node --check js/musicXmlParser.js && node --test tests/musicXmlParser.test.js</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add imported library registration boundary</name>
  <files>js/importedScoreLibrary.js, js/simplePatternLoader.js, tests/musicXmlParser.test.js</files>
  <read_first>
    js/simplePatternLoader.js
    js/importedScoreStore.js
    js/musicXmlParser.js
    .planning/phases/05-musicxml-import-and-practice-ux/05-UI-SPEC.md
  </read_first>
  <action>Create js/importedScoreLibrary.js with helpers createImportedScoreRecord, assignDuplicateImportedTitle, registerImportedScore, removeImportedScore, and listCompleteScoreOptions. Extend SimplePatternLoader only at the existing registration/rejected-source boundary so sourceType musicxml participates in getPatternOptions, getDiagnosticsForSource, validation summaries, and complete-score filtering. Do not remove support for built-in short pedagogical patterns; keep them available internally while making complete score-like entries the default Phase 5 library surface per D-01.</action>
  <acceptance_criteria>
    Duplicate names become "Title (2)", then "Title (3)" with no replacement.
    Removed imported score unregisters from valid and rejected source maps without touching built-ins.
    Complete-score option filtering can hide short patterns from the default library while preserving existing loader APIs.
  </acceptance_criteria>
  <done>Acceptance criteria pass and the automated verification command succeeds.</done>
  <verify>
    <automated>node --check js/importedScoreLibrary.js js/simplePatternLoader.js && node --test tests/musicXmlParser.test.js</automated>
  </verify>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| file text to parser | Untrusted XML enters parser logic |
| parser to loader | Accepted/rejected source records enter app registry |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Elevation of Privilege | js/musicXmlParser.js | mitigate | Parse as inert XML data only; prohibit eval, Function, user script imports, and remote user code. |
| T-05-02-02 | Tampering | js/importedScoreLibrary.js | mitigate | Use stable source IDs and suffix duplicate titles rather than replacing records silently. |
| T-05-02-03 | Repudiation | diagnostics | mitigate | Preserve sourceId, sourceType, code, path, and message for every import rejection. |
</threat_model>

<verification>
Run node --test tests/musicXmlParser.test.js and npm test.
</verification>

<success_criteria>
Strict MusicXML import core accepts a small supported score-partwise fixture, rejects unsupported or malformed files with structured diagnostics, and registers imported score records through the existing loader boundary without executing user content.
</success_criteria>

<output>
After completion, create .planning/phases/05-musicxml-import-and-practice-ux/05-02-SUMMARY.md
</output>
